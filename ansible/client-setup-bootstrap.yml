---
- name: Start bootstrapping node
  hosts: tag_Name_ST-boot
  remote_user: ubuntu # key is defined in ansible.cfg 
  gather_facts: false
  roles:
    - role: docker_install
      tags:
        cpp
        go
    - role: common
      tags:
        cpp
        go
    - boot
