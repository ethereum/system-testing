---
- name: Terminate all clients except the log server
  hosts: security_group_system-testing:!tag_Name_elarch # this is provided by ec2.py
  remote_user: ubuntu # private key defined via ansible.cfg 
  gather_facts: false
  roles:
      - aws

  tasks:
      - include: roles/aws/tasks/terminate_ec2.yml
